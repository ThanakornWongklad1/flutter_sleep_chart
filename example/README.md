# hypnogram_chart example

Demonstrates `HypnogramChart` with custom `stageStyles` (colors, labels, a
taller Deep row) and a custom `HypnogramTooltipConfig`.

## Run it

```
cd example
flutter run
```
